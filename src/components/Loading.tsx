import React from 'react'
import { View } from 'react-native'

const Loading = (): React.ReactNode => {
  return (
    <View className={'w-full h-full bg-yellow-50'}></View>
  )
}

export default Loading
