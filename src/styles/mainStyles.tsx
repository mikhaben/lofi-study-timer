import { StyleSheet } from 'react-native'

export const mainStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    backgroundColor: '#fff',
    color: 'red',
    flex: 1,
    justifyContent: 'center'
  },
  text: {
    color: 'red'
  }
})
